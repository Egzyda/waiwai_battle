# わいわいバトル 開発ルール

## ブランチ・デプロイ

- コード編集後は必ず `main` ブランチにプッシュ（マージ）する
- Cloudflare Pages が `main` へのプッシュを検知して自動デプロイする
- プッシュ後はブラウザで即確認できる状態を常に保つ

## コーディングルール

- ブラウザ標準ダイアログ（`alert` / `confirm` / `prompt`）の使用禁止
  - 代替：モーダル（`showModal()`）またはトースト（`showToast()`）を使用する
- テキストはひらがな中心で記述する
  - カタカナには `<ruby>` タグでひらがなルビを振る
- 感嘆符（`！`）はしかるべき場面のみ使用する（多用しない）
- BGM・SE は実装しない

## 画像ファイルの命名規則

- キャラクター立ち絵：`{id}_full.png`
- キャラクター顔画像：`{id}_face.png`
- 敵画像：`img/enemy/{id}.png`
- 背景画像：`img/bg/{name}.png`

## キャラクター追加手順

`data.js` の `CHARACTERS` オブジェクトにエントリを追加するだけで対応可能。
ステータス基準は Cross Legends の設計書（総価値450・許容±5）に従う。

## ミニゲーム追加手順

`minigames.js` に新しいミニゲームを追加し、`MINIGAMES` 配列に登録する。
ローテーション方式のため、配列に追加するだけで自動的にローテーションに組み込まれる。

## デプロイ先

- リポジトリ：https://github.com/Egzyda/waiwai_battle
- Cloudflare Pages 自動デプロイ（`main` ブランチ連動）
